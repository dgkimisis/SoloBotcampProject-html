@model GameShop.Models.SearchViewModel



<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <h2>GET INTO GAME ACTION</h2>

    @using (Html.BeginForm("Search", "Home", FormMethod.Post))
    {
        <fieldset>
            <p>Search the game you are looking for</p>
            <div>Name:@Html.TextBoxFor(m => m.Game.Name);</div> 
            @*<div>Genre:</div> @Html.DropDownListFor(ViewBag.genres);*@
            <div> Publish Year:</div> @Html.TextBoxFor(m => m.Game.PublishYear);
            <div> Violence Rating:</div> Html.TextBoxFor(m => m.max);
            <div> Company:</div> Html.TextBoxFor(m => m.Game.Company);
            <input type="submit" value="Submit" />
        </fieldset>
    }


    @*@using (Html.BeginForm("Display", "Home"))
    {
        <table class="table table-condensed">
           <thead>
             <tr>
                <th>Name</th>
                <th>Genre</th>
                <th>Publish Year</th>
                <th>Cost</th>
                <th>Violence Rating</th>
                <th>Company</th>
             </tr>
               <tbody>
                   <tr></tr>
               </tbody>
           </thead>

        </table>
        @Html.TextBox() 
    }*@



</body>
</html>
